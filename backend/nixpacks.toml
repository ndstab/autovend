[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "python311Packages.setuptools", "python311Packages.wheel"]

[phases.install]
cmds = [
  "npm ci --omit=dev",
  "pip install fastapi uvicorn httpx pydantic requests --quiet"
]

[start]
cmd = "node dist/index.js"
